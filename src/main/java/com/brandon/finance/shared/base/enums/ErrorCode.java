package com.brandon.finance.shared.base.enums;

public enum ErrorCode {
    RESOURCE_NOT_FOUND,
    BUSINESS_ERROR,
    VALIDATION_ERROR,
    INTERNAL_ERROR,
    UNAUTHORIZED,
    FORBIDDEN,
    TOKEN_EXPIRED,
    INVALID_TOKEN, 
    CONFLICT
    
}
